# URLShortener

A simple URL shortener built with Go.

## Features
- Shorten long URLs
- Redirect short URL → original
- Track visit counts
- In-memory storage (can extend with DB)

## Run
```bash
make run
